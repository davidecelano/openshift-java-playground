apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-undertow
  labels:
    app: metrics-undertow
spec:
  selector:
    matchLabels:
      app: metrics-undertow
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
